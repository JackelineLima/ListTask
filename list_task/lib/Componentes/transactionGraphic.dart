import 'package:flutter/material.dart';

class TransactionGraphic extends StatelessWidget {
  const TransactionGraphic({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // width: double.infinity,
      child: Card(
        child: Text('Gráfico'),
        elevation: 5,
      ),
    );
  }
}
